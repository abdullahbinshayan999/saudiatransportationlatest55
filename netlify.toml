[dev]
  # Use the same dev command as project and tell Netlify Dev which port Vite uses
  command = "npm run dev"
  targetPort = 5173
  port = 8888

[build]
  publish = "."
  functions = "netlify/functions"
